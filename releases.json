{
  "version": "1.11.0",
  "channel": "stable",
  "release_date": "2026-03-06",
  "contracts_version": "1.0.0",
  "changes": {
    "breaking": [],
    "features": [
      "feat(contracts): channel_registry.py — 41 EventContract entries, single source of truth for all Redis Streams channels",
      "feat(contracts): EventContract dataclass with producer/consumer/direction/status fields",
      "feat(tests): 38 parametrized channel alignment tests (prevents drift)",
      "feat(metrics): per-node Prometheus latency histogram (graph_node_duration_seconds)"
    ],
    "fixes": [
      "fix(async): removed nest_asyncio entirely — eliminated uvloop contamination chain",
      "fix(async): replaced SelectorEventLoop per-call pattern with asyncio.run() in intent_detection_node",
      "fix(async): Dockerfile --loop asyncio disables uvloop (clean asyncio policy)",
      "fix(concurrency): ExecutionGuard max_workers 4 → 8 (configurable via NODE_EXEC_MAX_WORKERS)",
      "fix(architecture): Orthodoxy = async audit log (not blocking gate), correlation_id = trace_id",
      "fix(architecture): removed TODO 'implement async verdict retrieval' — replaced with A+ decision"
    ]
  },
  "commits": [
    {
      "hash": "b68ee44",
      "message": "refactor(graph): Option A+ Phase 1-2 — contracts + performance tuning"
    },
    {
      "hash": "a640e65",
      "message": "refactor(graph): Option A+ Phase 0 — architectural audit + no-regret fixes"
    }
  ],
  "validation": [
    "29/29 hardening tests passing",
    "38/38 architectural channel alignment tests passing",
    "Zero nest_asyncio or SelectorEventLoop references in active code",
    "Phase 0-1-2 roadmap: 15/15 fixes completed (F0.1-F0.7, F1.1-F1.4, F2.1-F2.4)"
  ],
  "upgrade_instructions": "vit upgrade --channel stable",
  "checksum": {
    "algorithm": "sha256",
    "value": "TBD"
  }
}