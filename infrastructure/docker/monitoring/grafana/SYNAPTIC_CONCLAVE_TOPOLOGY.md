# Synaptic Conclave Topology (Mermaid)

<p class="kb-subtitle">Rendered diagram from <code>synaptic_conclave_topology.mmd</code>. MkDocs renders Mermaid only inside Markdown pages.</p>

```mermaid
--8<-- "infrastructure/docker/monitoring/grafana/synaptic_conclave_topology.mmd"
```

