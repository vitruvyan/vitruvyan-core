# Requirements

!!! warning "Placeholder"
    Define the official baseline (CPU/RAM/disk), supported OS, and required ports.

## Checklist (draft)

- Docker Engine + Docker Compose
- Access to required container registries (if private)
- Outbound network access for dependencies (optional, if mirrored)
- DNS/hostname strategy for `kb.vitruvyan.com` and internal services

