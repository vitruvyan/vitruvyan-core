# Requisiti

!!! warning "Placeholder"
    Definire la baseline ufficiale (CPU/RAM/disk), i sistemi operativi supportati e le porte richieste.

## Checklist (bozza)

- Docker Engine + Docker Compose
- Accesso ai registry container (se privati)
- Accesso di rete in uscita per le dipendenze (opzionale, se mirrorate)
- Strategia DNS/hostname per `kb.vitruvyan.com` e servizi interni

