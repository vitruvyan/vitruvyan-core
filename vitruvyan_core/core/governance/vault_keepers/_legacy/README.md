# Legacy Code Archive

This directory contains pre-refactoring code that has been replaced by the SACRED_ORDER_PATTERN compliant structure.

**Status**: Read-only archive (frozen)
**Purpose**: Historical reference, rollback safety

Do not modify files in this directory. For active development, use the parent directories (consumers/, governance/, etc.).

